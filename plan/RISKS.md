# RISKS.md — OffsideFleet · Top 10

Likelihood (L) / Impact (I): low·med·high. Every risk carries a mitigation AND a tripwire — the observable condition under which we change course, with the decision point named.

| # | Risk | L | I | Mitigation | Tripwire |
|---|------|---|---|------------|----------|
| 1 | **Replay-in-MVP blows the schedule** (lowest-confidence epic inside the ship gate; nondeterminism leaks from clock/streaming/ordering) | high | high | Replay-grade trace schema enforced from M1; M6 starts with the completeness validator so gaps surface on day 1; scope is engine-only (no polish) | Projected M6 >150% of 10 fd **at M5 exit or any point during M6** → pre-agreed fallback: ship MVP with recording only, engine moves to Phase B. Founder signs the fallback in DECISIONS.md |
| 2 | **Plaintext customer secrets breached** (SQLi, backup leak, admin compromise → working third-party creds) | med | high | Broker-only read path; structural redaction with CI grep test; workspace scoping; least-privilege DB roles | First external-partner credential (M7.S5): encrypt (~1 fd) or founder re-accepts on the record. Any suspected exposure → immediate rotation + envelope encryption lands before anything else ships |
| 3 | **Solo-founder pace vs 65-fd Phase A** (illness, fundraising, life; 192-fd total roadmap) | high | med | Strict milestone sequence — every milestone leaves a demoable, coherent product; honest 150% rule surfaces slips at once | Two consecutive milestones >150% → roadmap re-scope session; Phase B+ items move out before Phase A quality does |
| 4 | **Sandbox/tenant isolation failure** (cross-tenant read or container escape = the product's fatal wound) | low | high | 3-layer tenancy + adversarial CI suite blocking merge; hardened container profile + egress allowlist + sandbox-escape suite in CI; external security review before GA | ANY cross-tenant finding (test or prod) → all feature work stops until root-caused + suite extended. Arbitrary-code features approach (M15) → gVisor tripwire fires |
| 5 | **Google OAuth drags M4** (refresh/scope plumbing now; CASA verification cost/time at GA) | med | med | Test-mode (≤100 users) through design-partner phase; scopes minimized; verification scheduled as explicit GA gate, not discovered | Verification quoted >4 weeks or >$5k at GA planning → Workspace tools become "bring-your-own OAuth app" for Squadron+ or slip to post-GA; decision logged |
| 6 | **Model-cost margin erosion** (token pass-through mispriced; budget bugs leak spend) | med | med | Versioned price tables; pre+post budget enforcement with ≤1-step-overrun property test; daily reconciliation of usage_events vs Stripe vs ledger | Reconciliation drift >1% for 3 consecutive days, or gross margin <60% in any month → pricing/margin review before next billing cycle |
| 7 | **Platform giants commoditize the builder** (Anthropic/OpenAI agent platforms absorb the authoring layer) | high | med | Differentiate on ops discipline (replay, ledger, budgets, kill) + named-agent identity UX + multi-provider neutrality — not authoring alone | A major provider ships hosted traced/budgeted/replayable agents at platform pricing → reposition sprint: Fleet as the neutral multi-provider ops layer; roadmap re-cut at next phase boundary |
| 8 | **No-kill-list scope pressure** (founder overrides added replay+memory+3-tiers to MVP; pattern may continue) | high | med | DECISIONS.md logs every addition with its schedule price; milestone gates require sign-off; mid-milestone changes need a change note + ack (protocol) | Any single milestone absorbs >2 founder additions, or Phase A projected past March 2027 → mandatory descope session before next milestone starts |
| 9 | **Semantic memory quality undermines the persona illusion** (bad recall reads as a dumb agent; the feature exists to feel magical) | med | med | Retrieval steps visible in traces (debuggable); relevance thresholds; Voyage-class embeddings (ADR-011); notes tool gives agents explicit memory agency | Design partners report wrong-recall incidents in week one of use → memory ships behind a per-agent toggle default-off until retrieval precision is fixed |
| 10 | **App Platform constraints tax the control plane** (worker limits, SSE behavior, deploy quirks vs a droplet's flexibility) | med | low | Control plane is containerized and platform-agnostic; SSE validated in M3 spike before deep investment | SSE streaming or Procrastinate workers prove unreliable on App Platform by M3 → fall back to the two-droplet topology (ADR-005 alternative) in <1 fd; decision logged |

## Standing risk gates (from DECISIONS.md, restated so they can't be lost)
- **M5 exit:** M6 replay re-estimate + fallback decision.
- **M7.S5:** secrets encryption tripwire — before any external credential.
- **GA:** Google CASA verification; external security review (risk 4).
- **M15.S3:** gVisor evaluation before any third-party/arbitrary-code execution.
